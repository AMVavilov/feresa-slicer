# Preview stress slicing fixture

`preview_stress_medallion.stl` is a deterministic, project-owned regression fixture for incident
`FS-2026-08-25-POST-SLICE-OOM`. The original customer keychain cannot be committed because its
redistribution rights and private provenance are unknown. The synthetic fixture preserves the
properties relevant to the failure without copying the model or its artwork.

Incident evidence recorded during diagnosis:

- original input: binary STL, 3,702 triangles, about 58.1 × 50 × 4.8 mm;
- settings: 220 × 220 mm bed, 0.4 mm nozzle, 0.20 mm layer, PLA defaults;
- native result: 24 layers, about 46,700 toolpath moves, about 2.32 MB / 69,933 G-code lines;
- failure stage: native Orca slicing completed, then the app froze or was killed while preparing
  the WebView toolpath preview;
- pre-fix observation: application RSS peaked near 1,078 MiB with about 473 MiB swap;
- cause: full G-code and expanded JavaScript number arrays were retained across Kotlin and WebView,
  while model and toolpath WebViews could coexist;
- current automated expectation: three slices plus three production-WebView renders complete,
  peak application PSS stays at or below 512 MiB, and retained growth after warm-up stays at or
  below 128 MiB. The separate renderer process is still covered by process-death detection and the
  manual GPU-memory/visible-frame checks in the release matrix.

- Source and license: generated by this repository, `AGPL-3.0-only`.
- Generator: `scripts/generate-release-stress-fixture.py`.
- Command: `python3 scripts/generate-release-stress-fixture.py \
  app/src/androidTest/assets/release-fixtures/preview_stress_medallion.stl`.
- Expected geometry: 5,888 triangles; about 58 × 50 × 4.8 mm.
- Regression: the production pipeline and real Model-screen action must complete within their
  timeouts, repeated Orca slicing must stay inside the memory limits enforced by
  `PrePlaySlicingInstrumentedTest`, and the production WebView must report a rendered toolpath.

After regenerating the file, update and verify the SHA-256 value below before committing it:

```text
35e897f18beac0869db7a317eafa488e233f703731147c23d07501c518fb0da6  preview_stress_medallion.stl
```
